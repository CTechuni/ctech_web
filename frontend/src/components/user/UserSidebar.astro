---
// UserSidebar.astro - Pixel Perfect v2
const menuItems = [
    { icon: `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
  <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6A2.25 2.25 0 0 1 6 3.75h2.25A2.25 2.25 0 0 1 10.5 6v2.25a2.25 2.25 0 0 1-2.25 2.25H6a2.25 2.25 0 0 1-2.25-2.25V6ZM3.75 15.75A2.25 2.25 0 0 1 6 13.5h2.25a2.25 2.25 0 0 1 2.25 2.25V18a2.25 2.25 0 0 1-2.25 2.25H6A2.25 2.25 0 0 1 3.75 18v-2.25ZM13.5 6a2.25 2.25 0 0 1 2.25-2.25H18A2.25 2.25 0 0 1 20.25 6v2.25A2.25 2.25 0 0 1 18 10.5h-2.25a2.25 2.25 0 0 1-2.25-2.25V6ZM13.5 15.75a2.25 2.25 0 0 1 2.25-2.25H18a2.25 2.25 0 0 1 2.25 2.25V18A2.25 2.25 0 0 1 18 20.25h-2.25A2.25 2.25 0 0 1 13.5 18v-2.25Z" />
</svg>`, label: 'Inicio', path: '/user' },
    { icon: `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
  <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-5.197-5.197m0 0A7.5 7.5 0 105.196 5.196a7.5 7.5 0 0010.607 10.607z" />
</svg>`, label: 'Explorar Cursos', path: '/explore/courses' },
    { icon: `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
  <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5A2.25 2.25 0 0121 11.25v7.5" />
</svg>`, label: 'Calendario', path: '/user/calendar' },
    { icon: `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
  <path stroke-linecap="round" stroke-linejoin="round" d="M17.982 18.725A7.488 7.488 0 0 0 12 15.75a7.488 7.488 0 0 0-5.982 2.975m11.963 0a9 9 0 1 0-11.963 0m11.963 0A8.966 8.966 0 0 1 12 21a8.966 8.966 0 0 1-5.982-2.275M15 9.75a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" />
</svg>`, label: 'Mi Perfil', path: '/user/profile' },
    { icon: `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
  <path stroke-linecap="round" stroke-linejoin="round" d="M10.343 3.94c.09-.542.56-.94 1.11-.94h1.093c.55 0 1.02.398 1.11.94l.149.894c.07.424.384.764.78.93.398.164.855.142 1.205-.108l.737-.527a1.125 1.125 0 0 1 1.45.12l.773.774c.39.389.44 1.002.12 1.45l-.527.737c-.25.35-.272.806-.107 1.204.165.397.505.71.93.78l.893.15c.543.09.94.559.94 1.109v1.094c0 .55-.397 1.02-.94 1.11l-.894.149c-.424.07-.764.383-.929.78-.165.398-.143.854.107 1.204l.527.738c.32.447.269 1.06-.12 1.45l-.774.773a1.125 1.125 0 0 1-1.449.12l-.738-.527c-.35-.25-.806-.272-1.203-.107-.398.165-.71.505-.781.929l-.149.894c-.09.542-.56.94-1.11.94h-1.094c-.55 0-1.019-.398-1.11-.94l-.148-.894c-.071-.424-.384-.764-.781-.93-.398-.164-.854-.142-1.204.108l-.738.527c-.447.32-1.06.269-1.45-.12l-.773-.774a1.125 1.125 0 0 1-.12-1.45l.527-.737c.25-.35.272-.806.108-1.204-.165-.397-.506-.71-.93-.78l-.894-.15c-.542-.09-.94-.56-.94-1.109v-1.094c0-.55.398-1.02.94-1.11l.894-.149c.424-.07.765-.383.93-.78.165-.398.143-.854-.108-1.204l-.526-.738a1.125 1.125 0 0 1 .12-1.45l.773-.773a1.125 1.125 0 0 1 1.45-.12l.737.527c.35.25.807.272 1.204.107.397-.165.71-.505.78-.929l.15-.894Z" />
  <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" />
</svg>`, label: 'Ajustes', path: '/user/settings' },
];
---

<aside class="user-sidebar">
    <!-- Logo -->
    <div class="sidebar-brand">
        <div class="brand-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="white" width="20" height="20">
                <path fill-rule="evenodd" d="M9.315 7.584C12.195 3.883 16.695 1.5 21.75 1.5a.75.75 0 01.75.75c0 5.056-2.383 9.555-6.084 12.436A6.75 6.75 0 019.75 22.5a.75.75 0 01-.75-.75v-4.131A15.838 15.838 0 016.382 15H2.25a.75.75 0 01-.75-.75 6.75 6.75 0 017.815-6.666zM15 6.75a2.25 2.25 0 100 4.5 2.25 2.25 0 000-4.5z" clip-rule="evenodd"/>
                <path d="M5.26 17.242a.75.75 0 10-.897-1.203 5.243 5.243 0 00-2.05 5.022.75.75 0 00.625.627 5.243 5.243 0 005.022-2.051.75.75 0 10-1.202-.897 3.744 3.744 0 01-3.008 1.51c0-1.23.592-2.323 1.51-3.008z"/>
            </svg>
        </div>
        <span class="brand-name">CTech</span>
        <button class="collapse-btn" id="collapseBtn" title="Colapsar sidebar">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="14" height="14">
                <path fill-rule="evenodd" d="M7.72 12.53a.75.75 0 010-1.06l7.5-7.5a.75.75 0 111.06 1.06L9.31 12l6.97 6.97a.75.75 0 11-1.06 1.06l-7.5-7.5z" clip-rule="evenodd"/>
            </svg>
        </button>
    </div>

    <!-- Profile Card -->
    <div class="profile-card">
        <div class="profile-avatar" id="sidebarAvatar">--</div>
        <div class="profile-info">
            <span class="profile-name" id="sidebarName">Cargando...</span>
            <span class="profile-community" id="sidebarCommunity">--</span>
        </div>
        <div class="profile-badge">Estudiante</div>
    </div>

    <!-- Navigation -->
    <nav class="sidebar-nav">
        {menuItems.map(item => (
            <a href={item.path} class="nav-item" data-path={item.path}>
                <span class="nav-icon"><Fragment set:html={item.icon} /></span>
                <span class="nav-label">{item.label}</span>
            </a>
        ))}
    </nav>

    <!-- Footer -->
    <div class="sidebar-footer">
        <button id="logoutBtn" class="logout-btn">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="18" height="18">
                <path fill-rule="evenodd" d="M7.5 3.75A1.5 1.5 0 006 5.25v13.5a1.5 1.5 0 001.5 1.5h6a1.5 1.5 0 001.5-1.5V15a.75.75 0 011.5 0v3.75a3 3 0 01-3 3h-6a3 3 0 01-3-3V5.25a3 3 0 013-3h6a3 3 0 013 3V9A.75.75 0 0115 9V5.25a1.5 1.5 0 00-1.5-1.5h-6zm10.72 4.72a.75.75 0 011.06 0l3 3a.75.75 0 010 1.06l-3 3a.75.75 0 11-1.06-1.06l1.72-1.72H9a.75.75 0 010-1.5h10.94l-1.72-1.72a.75.75 0 010-1.06z" clip-rule="evenodd"/>
            </svg>
            <span>Salir</span>
        </button>
    </div>
</aside>

<style>
    .user-sidebar {
        width: 260px;
        height: 100vh;
        position: fixed;
        left: 0;
        top: 0;
        background: #0d1117;
        display: flex;
        flex-direction: column;
        z-index: 1000;
        overflow: hidden;
        transition: width 0.3s ease, transform 0.3s ease;
    }

    /* ── Collapsed State ───────────────────────── */
    :global(.sidebar-collapsed) .user-sidebar {
        width: 70px;
    }

    :global(.sidebar-collapsed) .brand-name,
    :global(.sidebar-collapsed) .profile-info,
    :global(.sidebar-collapsed) .profile-badge,
    :global(.sidebar-collapsed) .nav-label,
    :global(.sidebar-collapsed) .logout-btn span {
        display: none;
    }

    :global(.sidebar-collapsed) .sidebar-brand { justify-content: center; padding: 20px 0; }
    :global(.sidebar-collapsed) .collapse-btn { position: absolute; right: -10px; top: 25px; transform: rotate(180deg); background: #007bff; color: white; border: 2px solid #0d1117; }
    :global(.sidebar-collapsed) .profile-card { grid-template-columns: 1fr; justify-items: center; padding: 10px 0; }
    :global(.sidebar-collapsed) .nav-item { justify-content: center; padding: 12px 0; }

    /* ── Mobile State ─────────────────────────── */
    @media (max-width: 1024px) {
        .user-sidebar {
            transform: translateX(-100%);
        }
        :global(.sidebar-open) .user-sidebar {
            transform: translateX(0);
        }
        .collapse-btn { display: none; }
    }

    /* ── Brand ─────────────────────────────────── */
    .sidebar-brand {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 20px 16px 16px;
        border-bottom: 1px solid rgba(255,255,255,0.06);
        position: relative;
    }

    .brand-icon {
        width: 34px;
        height: 34px;
        background: #007bff;
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

    .brand-name {
        font-size: 1.1rem;
        font-weight: 800;
        color: white;
        letter-spacing: -0.02em;
        flex: 1;
    }

    .collapse-btn {
        width: 22px;
        height: 22px;
        background: rgba(255,255,255,0.08);
        border: none;
        border-radius: 50%;
        color: rgba(255,255,255,0.5);
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.2s;
        flex-shrink: 0;
    }
    .collapse-btn:hover { background: rgba(255,255,255,0.15); color: white; }

    /* ── Profile Card ──────────────────────────── */
    .profile-card {
        margin: 14px 12px;
        background: #007bff;
        border-radius: 12px;
        padding: 12px 14px;
        display: grid;
        grid-template-columns: 40px 1fr;
        grid-template-rows: auto auto;
        column-gap: 10px;
        align-items: center;
    }

    .profile-avatar {
        width: 40px;
        height: 40px;
        background: rgba(255,255,255,0.25);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 0.85rem;
        color: white;
        grid-row: 1 / 3;
        letter-spacing: 0.5px;
    }

    .profile-info {
        display: flex;
        flex-direction: column;
        gap: 1px;
    }

    .profile-name {
        font-size: 0.88rem;
        font-weight: 700;
        color: white;
        line-height: 1.2;
    }

    .profile-community {
        font-size: 0.72rem;
        color: rgba(255,255,255,0.75);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .profile-badge {
        grid-column: 2;
        background: rgba(255,255,255,0.2);
        color: white;
        font-size: 0.65rem;
        font-weight: 700;
        padding: 2px 8px;
        border-radius: 20px;
        width: fit-content;
        letter-spacing: 0.03em;
        text-transform: uppercase;
        margin-top: 4px;
    }

    /* ── Navigation ────────────────────────────── */
    .sidebar-nav {
        flex: 1;
        padding: 6px 10px;
        display: flex;
        flex-direction: column;
        gap: 2px;
        overflow-y: auto;
        scrollbar-width: none;
    }
    .sidebar-nav::-webkit-scrollbar { display: none; }

    .nav-item {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 9px 12px;
        color: #8b9ab4;
        text-decoration: none;
        border-radius: 8px;
        font-size: 0.855rem;
        font-weight: 500;
        transition: all 0.18s ease;
        cursor: pointer;
    }

    .nav-icon {
        width: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

    .nav-icon :global(svg) {
        width: 17px;
        height: 17px;
    }

    .nav-item:hover {
        color: #e2e8f0;
        background: rgba(255,255,255,0.06);
    }

    .nav-item.active {
        background: rgba(0, 115, 255, 0.15);
        color: #007bff;
        font-weight: 600;
    }

    .nav-item.active .nav-icon :global(svg) {
        color: #007bff;
    }

    /* ── Footer / Logout ───────────────────────── */
    .sidebar-footer {
        padding: 12px 10px 20px;
        border-top: 1px solid rgba(255,255,255,0.06);
    }

    .logout-btn {
        display: flex;
        align-items: center;
        gap: 10px;
        width: 100%;
        padding: 9px 12px;
        background: transparent;
        border: none;
        color: #8b9ab4;
        cursor: pointer;
        border-radius: 8px;
        font-size: 0.855rem;
        font-weight: 500;
        transition: all 0.18s;
        font-family: inherit;
    }

    .logout-btn:hover {
        background: rgba(255,255,255,0.06);
        color: #f87171;
    }
</style>

<script>
    import { authManager, AuthManager } from "../../utils/auth.js";

    // 1. Cargar datos del perfil
    async function loadSidebarProfile() {
        const cached = authManager.getUser();
        const avatar = document.getElementById('sidebarAvatar');
        const name = document.getElementById('sidebarName');
        const community = document.getElementById('sidebarCommunity');
        if (cached) {
            if (avatar) avatar.textContent = (cached.name_user || '--').substring(0, 2).toUpperCase();
            if (name) name.textContent = cached.name_user || 'Usuario';
            if (community) community.textContent = cached.community_name || 'Sin Comunidad';
        }
        try {
            const fresh = await AuthManager.fetch('/users/me');
            if (fresh) {
                authManager.setAuthData(authManager.getToken(), { ...fresh, role: 'user' });
                if (avatar) avatar.textContent = (fresh.name_user || '--').substring(0, 2).toUpperCase();
                if (name) name.textContent = fresh.name_user || 'Usuario';
            }
        } catch (e) { /* silencioso */ }
    }

    // 2. Logout
    document.getElementById('logoutBtn')?.addEventListener('click', () => {
        authManager.logout();
    });

    // 3. Marcar link activo
    const currentPath = window.location.pathname;
    document.querySelectorAll('.nav-item').forEach(link => {
        const href = link.getAttribute('href')?.split('?')[0];
        const isActive = href === currentPath || (href !== '/user' && currentPath.startsWith(href));
        if (isActive) link.classList.add('active');
    });

    // 4. Toggle Sidebar Event
    document.getElementById('collapseBtn')?.addEventListener('click', () => {
        window.dispatchEvent(new CustomEvent('toggleSidebar'));
    });

    // Inicializar
    loadSidebarProfile();
</script>

